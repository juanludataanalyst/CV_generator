# 🎯 CV Adapter Project Initialization Prompt (V1) *Date: April 06, 2025* ## Project Overview This project develops a tool to convert a CV from PDF format into a standardized, ATS-friendly CV in PDF format. For Version 1 (V1), the focus is on extracting text from a PDF CV, using an LLM to parse it into the JSON Resume standard, and generating a simple, professional ATS-optimized CV without altering its content beyond structuring. Future versions will add job description adaptation and a UI. ### Core Objectives (V1) - Read a CV in PDF format from a local file. - Extract text from the PDF accurately. - Use an LLM to parse the extracted text into the JSON Resume standard format. - Generate a minimalistic, ATS-friendly CV in PDF format with the original content. ### Workflow (V1) 1. **Input**: A PDF CV file located in the project directory (e.g., `input_cv.pdf`). 2. **Text Extraction**: Convert the PDF to plain text using `pdfminer.six`. 3. **Parsing**: Use an LLM (e.g., Grok by xAI) to adapt the extracted text into JSON Resume format, preserving original content. 4. **Output**: Generate a simple ATS-friendly PDF CV using `reportlab`. ### Future Versions - **V2**: Add job description input and LLM adaptation to tailor the CV content, plus a Streamlit UI. - **V3**: Agentize the CV editing process for interactive modifications. ## 📚 Required Documentation Developers must review the following resources before starting: 1. **PDF to Text Extraction Library** - **Library**: `pdfminer.six`. - **Documentation**: [pdfminer.six - Converting PDF to Text](https://pdfminersix.readthedocs.io/en/latest/topic/converting_pdf_to_text.html) - Focus on the `extract_text` function for converting PDFs to plain text. Note Windows-specific installation steps. 2. **JSON Resume Standard** - **Documentation**: [JSON Resume Schema](https://jsonresume.org/schema) - Official schema documentation with detailed field descriptions (e.g., `basics`, `work`, `education`, `skills`). Use this as the target format for LLM parsing. 3. **LLM for Parsing Text to JSON Resume** - **Approach**: Use an LLM (e.g., Grok by xAI) to convert unstructured CV text into JSON Resume format. - **Documentation**: No external doc needed; rely on prompt engineering (see `PLANNING.md` for examples). 4. **Generating a Standard, Professional ATS-Friendly CV** - **Resource**: [GitHub - sliday/resume-job-matcher](https://github.com/sliday/resume-job-matcher) - Review the `output/resume.txt` example for ATS-friendly formatting (plain text, no graphics, keyword-focused). - **Library**: `reportlab` for PDF generation ([ReportLab Docs](https://www.reportlab.com/docs/reportlab-userguide.pdf)) - Focus on minimalistic PDF creation with Helvetica font. 5. **Python Project Structure Best Practices** - **Resource**: [Python Project Layout Guide](https://realpython.com/python-application-layouts/) - Follow the "Standard Python Project" layout, adapted for Windows compatibility. - **Structure**: ``` cv_adapter/ ├── src/ # Main source code │ ├── cv_extraction.py # PDF to text │ ├── cv_parser.py # Text to JSON Resume via LLM │ ├── ats_generator.py # JSON to ATS PDF ├── tests/ # Unit tests │ ├── test_extraction.py │ ├── test_parser.py │ └── test_generator.py ├── input_cv.pdf # Example input CV ├── PLANNING.md # Architecture and goals ├── TASK.md # Task tracking ├── README.md # Setup and usage └── requirements.txt # Dependencies ``` ## 🔗 Inspirational Projects - **Resume Job Matcher**: [GitHub - sliday/resume-job-matcher](https://github.com/sliday/resume-job-matcher) - Reference for ATS-friendly CV formatting and parsing ideas (e.g., simple text structure). ## 🛠 Initial Setup Instructions (Windows) 1. **Create Project Directory**: - Open a Command Prompt and run: `mkdir cv_adapter && cd cv_adapter`. - Set up the structure as outlined above. 2. **Install Dependencies**: - Create `requirements.txt` with: ``` pdfminer.six==20231228 reportlab==4.0.9 pytest==8.1.1 black==24.3.0 ``` - Run `pip install -r requirements.txt` in Command Prompt. - **Windows Note**: Ensure Python is added to PATH; use `py -m pip` if `pip` fails. 3. **Initialize Documentation**: - Manually create `PLANNING.md` with architecture details (e.g., module responsibilities, LLM prompt examples, Windows considerations). - Manually create `TASK.md` with initial tasks: ``` - Set up PDF extraction with pdfminer.six - April 06, 2025 - Parse text to JSON Resume using LLM - April 06, 2025 - Generate ATS PDF - April 06, 2025 ``` - Manually create `README.md` with: ``` # CV Adapter (V1) Converts a PDF CV to an ATS-friendly PDF using JSON Resume standard with LLM parsing. ## Setup (Windows) 1. Clone repo or create folder: `mkdir cv_adapter && cd cv_adapter`. 2. Install dependencies: `pip install -r requirements.txt`. 3. Place CV as `input_cv.pdf` in root folder. 4. Run: `py src/main.py`. ``` 4. **First Code Stub**: - Create `src/main.py`: ```python from cv_extraction import extract_cv_text from cv_parser import parse_to_json_resume from ats_generator import generate_ats_pdf def main(): pdf_path = "input_cv.pdf" output_path = "output_cv.pdf" text = extract_cv_text(pdf_path) json_cv = parse_to_json_resume(text) # Uses LLM internally generate_ats_pdf(json_cv, output_path) print(f"ATS CV generated at {output_path}") if __name__ == "__main__": main() ``` - Create `src/cv